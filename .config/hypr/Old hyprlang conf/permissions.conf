### Permissions

# https://wiki.hyprland.org/Configuring/Permissions/
# Permission change require a Hyprland restart and are not appied on-the-fly dor security reasons

# ecoststem{
#	enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow
